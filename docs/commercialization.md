# 商用化チェックリスト

2026-08-23 のコードベース調査を起点に、商用公開までに解消すべき項目をまとめたもの。  
解消したら `[ ]` を `[x]` に変え、必要なら「対応メモ」を追記する。

関連: [`design.md`](design.md) / [`infra.md`](infra.md) / [`local-dev.md`](local-dev.md)

---

## 進捗サマリ

| 優先度 | 残件数の目安 | 備考 |
|--------|-------------|------|
| P0 公開ブロッカー | 法務・削除・Nominatim・JAF・海しる Secret | OpenWeather・潮位・地理院タイルは完了。Secret 設定残り |
| P1 信頼性・セキュリティ | 認証・CORS・バックアップ等 | |
| P2 プロダクト・運用 | 免責・同意 UX・監視等 | |
| P3 その他 | OSS 表記・課金基盤など | 有料化するとき必須が増える |

---

## P0 — 公開前に必須

### 1. 法的ドキュメント

収集データ（明示対象）:

| データ | 保存先 |
|--------|--------|
| メールアドレス | Cognito |
| GPS 座標・釣り場 | DynamoDB / IndexedDB |
| 写真 | S3 |
| 魚種・タックル・記録 | DynamoDB / IndexedDB |

位置情報（釣り場）はセンシティブ。取得目的・保存期間・削除方法の明示が個人情報保護法（APPI）上重要。

- [ ] プライバシーポリシー作成（取得・利用・保存・第三者提供・保存期間・削除）
- [ ] 利用規約作成
- [ ] アプリ内からポリシー / 規約へのリンク（ログイン・マイページ等）
- [ ] 新規登録時の同意チェック（規約・プライバシー）
- [ ] 特定商取引法に基づく表記（**有料プラン・サブスクを出す場合**）
- [ ] 返金ポリシー（有料化する場合）

**対応メモ:**

-

### 2. アカウント削除・データエクスポート

現状: メール変更・パスワード変更のみ。退会・エクスポートなし。

- [ ] 退会（アカウント削除）UI + 確認フロー
- [ ] Cognito ユーザー削除
- [ ] DynamoDB の当該ユーザー全レコード削除
- [ ] S3 写真削除
- [ ] IndexedDB / 端末データのクリア案内（または自動削除）
- [ ] データエクスポート（JSON 等）— ポータビリティ

**対応メモ:**

-

### 3. 外部 API・データの商用許諾

#### 天気（OpenWeatherMap）

- [x] Open-Meteo（非商用無料）からの離脱
- [x] Lambda プロキシ + DynamoDB キャッシュ（`GET /weather/current`）
- [x] 帰属表示（`WeatherAttribution`）
- [x] GitHub Secret `OPENWEATHER_API_KEY` を本番設定し、動作確認済み（2026-08-24）
- [ ] 無料枠（月間・分間）の監視 or アラート方針

**対応メモ:** 現在値のみ取得。編集時は保存済み天気を維持。

#### 地図タイル（国土地理院）

現状: `CoordinatePickerMap` / `RecordsMap` で地理院タイル（標準地図）をリアルタイム表示。日本のみ。

- [x] OSM 公式タイル直叩きをやめる
- [x] 選択プロバイダーの利用規約・帰属表示を満たす（地図コントロールに[地理院タイル一覧](https://maps.gsi.go.jp/development/ichiran.html)へのリンク）
- [x] 公式サーバーのリアルタイム読み込み（出典明示のみ・申請不要の範囲）

**対応メモ:**

海外対応する場合は別プロバイダーが必要。

#### 逆ジオコーディング（Nominatim 公式）

現状: `nominatim.openstreetmap.org`（`place.ts`）。User-Agent 未設定・キャッシュなし。

- [ ] 公式 Nominatim 直叩きをやめる（商用プロバイダー or 自前）
- [ ] 適切な識別ヘッダ / レート制限 / キャッシュ

**対応メモ:**

-

#### 潮位（海しる潮汐推算 — 案 A 採用）

- [x] 方針決定: **A. 海しる潮汐推算 API**（海上保安庁）
- [x] tide736 クライアント直叩きを廃止
- [x] `harbors.json`（tide736 由来）削除 → `api/src/tideStations.json`（公式推算点 313）
- [x] Lambda `GET /tide/current?lat=&lng=&at=` + 日次系列キャッシュ（WeatherCacheTable 共用）
- [x] 月齢・潮種・月相は内製（`api/src/moon.ts`）
- [x] 帰属表示（`TideAttribution` — 天文潮位＝予測値の文言含む）
- [ ] GitHub Secret `MSIL_SUBSCRIPTION_KEY` を本番設定し動作確認
- [ ] 定常利用向けの**個別サブスクリプションキー**申請（試用キーは変更・停止されうる）
- [ ] 推算点マスタの更新運用（`npm run build:tide-stations`）

**対応メモ:**

- API: `https://api.msil.go.jp/tide-prediction/v3`（`/station`, `/data?stationCode=&date=YYYYMMDD`）
- ポータル: https://portal.msil.go.jp/howtouse
- 航海用途には潮汐表を使う旨が API 説明にあり。アプリは釣り記録用の参考値として免責表示。
- 未採用: B 調和定数内製 / C 年次バッチ / D 潮種のみ
#### 魚種マスタ（JAF リスト）

現状: `scripts/build_fish_species.py` が鹿児島大学公開の JAF リストを取得し `fish-species.json` に同梱。

- [ ] 商用再配布・アプリ同梱の許諾を権利者に確認
- [ ] 不可の場合: 自社管理リスト / 別ライセンスデータへ切替
- [ ] 出典・ライセンス表記（許諾された場合）

**対応メモ:**

-

---

## P1 — 信頼性・セキュリティ

### 4. 認証まわり

- [ ] パスワードリセット（忘れた場合）フロー
- [ ] （任意）MFA
- [ ] Cognito パスワードポリシー強化の要否判断（大文字・記号など）

**対応メモ:** 登録時の規約同意は P0-1 と合わせて実施。

### 5. API / インフラ hardening

- [ ] API Gateway CORS を本番ドメインに限定（現状 `*`）
- [ ] S3 Media バケット CORS を本番ドメインに限定
- [ ] レート制限（API Gateway / WAF 等）
- [ ] WAF（必要なら）
- [ ] DynamoDB PITR / バックアップ有効化
- [ ] `docs/infra.md` 等からの本番アカウント ID・URL・Cognito ID の扱い見直し（公開リポジトリのリスク）

**対応メモ:**

-

### 6. 写真アップロード検証

- [ ] `photoKey` が `userId/` 配下であることをサーバ側で検証
- [ ] アップロードサイズ上限
- [ ] Content-Type 検証（JPEG 等）

**対応メモ:**

-

### 7. 同期の信頼性

- [ ] オフライン削除が API 失敗時にサーバに残る問題の解消（リトライ / キュー）
- [ ] 端末間競合の方針明確化（現状は last-write-wins）と必要なら改善
- [ ] 初回移行フラグ（`localStorage` 依存）の堅牢化検討

**対応メモ:**

-

---

## P2 — プロダクト・運用

### 8. 免責・精度表示

- [x] 潮位が天文潮位（予測）であることの UI 明示（`TideAttribution`）
- [ ] 天気・潮汐を釣行判断の絶対根拠にしない免責（利用規約側でも明記）

**対応メモ:**

-

### 9. 位置情報の同意 UX

- [ ] 初回利用時の目的説明
- [ ] 設定 / マイページからの再確認導線
- [ ] ストア公開時の位置情報用途説明文（App Store / Play）

**対応メモ:**

-

### 10. プロジェクト自身のライセンス

- [ ] `LICENSE` の方針決定（クローズド / OSS 等）
- [ ] 必要なら `LICENSE` ファイル追加
- [ ] OSS 依存の NOTICES / 帰属表記（推奨）

**対応メモ:**

-

### 11. 運用基盤

- [ ] CloudWatch アラーム（5xx・スロットル・課金急増など）
- [ ] ステージング（dev スタック）の要否判断・導入
- [ ] カスタムドメイン + ACM + Route 53
- [x] 本番デプロイを GitHub Actions に一本化（手動 `deploy:infra` / `deploy:aws` 削除済み）
- [x] `main` push / `workflow_dispatch` でのデプロイ経路

**対応メモ:** OIDC 信頼ポリシーの immutable sub 対応は `infra.md` 参照。

---

## P3 — 依存・その他

- [x] 画像再エンコードで EXIF（位置）除去（canvas 経由）— 現状問題なし、回帰に注意
- [x] 開発用シードは `import.meta.env.DEV` ガード — 本番に出ないことを維持
- [ ] OSS 依存のライセンス一覧（NOTICES）を用意
- [ ] 有料化する場合: Stripe 等の決済・サブスク管理
- [ ] 有料化する場合: プラン・制限・課金失敗時の UX

**対応メモ:**

-

---

## 推奨着手順（再掲）

公開ブロッカー:

1. プライバシーポリシー・利用規約 + アプリ内リンク・同意
2. アカウント削除 + 全データ消去（+ できればエクスポート）
3. Nominatim の商用プロバイダー化（地図タイルは地理院済み）
4. JAF 魚種リストの許諾確認または置換
5. 海しる `MSIL_SUBSCRIPTION_KEY` の本番 Secret 設定・個別キー申請（実装は完了）
6. OpenWeather 枠監視（Secret・動作確認は完了）

品質向上:

8. パスワードリセット
9. CORS 制限・レート制限・WAF・PITR
10. 写真キー検証・サイズ制限
11. 利用規約側の気象・潮汐免責
12. 有料化するなら特商法・返金・決済

---

## 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-08-23 | 初回洗い出し（チャット） |
| 2026-08-24 | 本チェックリスト化。OpenWeather 移行・CI 一本化を完了扱い |
| 2026-08-24 | OpenWeather Secret 動作確認済み。潮位の代替候補（海しる / 調和定数内製等）を追記 |
| 2026-08-24 | 潮位を海しる潮汐推算 API（案 A）へ実装移行。tide736 / harbors.json 削除 |
| 2026-08-24 | 地図タイルを OSM 公式から国土地理院（標準地図）へ差し替え。日本のみ |
